//
//  Resouces.swift
//  UnsplashTestApp
//
//  Created by Vladislav Avrutin on 18.09.2024.
//

import UIKit

enum Resources {
    enum Colors {
        static let viewBackground = UIColor.color(light: .white, dark: .gray)
        static let textColor = UIColor.color(light: .black, dark: .white)
        static let tabBarBackgroundColor = UIColor.color(light: .gray, dark: .white)
    }
}
